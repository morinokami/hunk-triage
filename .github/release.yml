# Categories for the pull request list that `gh release create --generate-notes` appends to release notes.
changelog:
  categories:
    - title: Changes
      labels: ["*"]
      exclude:
        labels: [dependencies]
    - title: Dependencies
      labels: [dependencies]
